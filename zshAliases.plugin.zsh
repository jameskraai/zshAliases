# Composer
alias cdu="composer dump-autoload -o"
alias phpcs="./vendor/bin/phpcs"

# Vagrant
alias vup="vagrant up"
alias vst="vagrant status"
alias vsh="vagrant ssh"
alias vhh="vagrant halt"
alias vss="vagrant suspend"

# History
alias h="fc -il 1"
alias history="fc -il 1"

# Prompt
alias k="clear"
alias rm="rm -i"
